# 📊 本地数据源集成指南

快速让StockPy跑起来，使用本地通达信/同花顺数据。

## 🎯 核心思路

```
通达信/同花顺 → 导出CSV → 放入data目录 → StockPy自动读取 → 运行
```

---

## 📋 步骤1: 准备本地数据

### 选项A: 使用通达信导出数据 ⭐推荐

#### 1.1 打开通达信客户端
- 进入 "行情" → 选择需要的股票
- 右键选择 "导出数据" 或 "保存数据"

#### 1.2 数据导出设置
- **时间范围**: 选择 "所有历史数据" 或 "最近1年"
- **导出周期**: 选择 "日线"
- **导出路径**: 选择任意文件夹（我们待会会整理）

#### 1.3 导出数据格式
通达信通常导出为`.txt`或`.csv`，需要包含:
```
日期,开盘,最高,最低,收盘,成交量,成交额,涨跌幅
2024-12-01,3000,3100,2950,3050,1000000,50000000000,1.67
2024-12-02,3050,3150,3000,3100,1100000,55000000000,1.64
```

---

### 选项B: 使用同花顺导出数据

#### 2.1 打开同花顺
- 进入 "数据→导出" → 选择股票
- 选择 "K线数据"
- 周期选择: **日线 (D)**

#### 2.2 保存数据
- 导出为 CSV 格式
- 文件命名规范: `{股票代码}_{周期}.csv`
  - 示例: `sh000001_D.csv`（上证指数日线）
  - 示例: `sz000858_D.csv`（五粮液日线）

---

## 🗂️ 步骤2: 整理数据文件

### 目录结构

创建如下目录结构（您已配置的 `D:\通达信`）:

```
D:\通达信\
├── sh000001_D.csv          # 上证指数日线
├── sz399001_D.csv          # 深证成指日线
├── sh600000_D.csv          # 浦发银行日线
├── sz000858_D.csv          # 五粮液日线
├── sh000001_W.csv          # 上证指数周线（可选）
└── stocks.csv              # 股票列表
```

### CSV文件标准格式

**文件名规范**: `{代码}_{周期}.csv`
- 代码: `sh` (上海) 或 `sz` (深圳) + 六位数字
- 周期: `D` (日) / `W` (周) / `M` (月)

**列名顺序** (必须包含):
```
date,code,open_price,high_price,low_price,close_price,volume,amount,change_percent
```

**示例数据** (sh000001_D.csv):
```csv
date,code,open_price,high_price,low_price,close_price,volume,amount,change_percent
2024-12-01,sh000001,3000.00,3100.00,2950.00,3050.00,1000000,50000000000,1.67
2024-12-02,sh000001,3050.00,3150.00,3000.00,3100.00,1100000,55000000000,1.64
2024-12-03,sh000001,3100.00,3180.00,3080.00,3150.00,1050000,52500000000,1.61
```

### stock.csv 股票列表

```csv
code,name,market,industry,is_active
sh000001,上证指数,SH,指数,1
sz399001,深证成指,SZ,指数,1
sh600000,浦发银行,SH,银行,1
sz000858,五粮液,SZ,食品饮料,1
sh601998,中国银行,SH,银行,1
```

---

## 🔧 步骤3: 配置项目

### 3.1 修改 `.env` 文件

```bash
cd backend
cp .env.example .env
```

编辑 `.env` 文件:
```ini
# 数据库
DATABASE_URL=sqlite:///./stockpy.db

# 数据源（重点配置）
DATA_SOURCE_TYPE=local
LOCAL_DATA_PATH=D:\通达信

# 其他配置保持默认
DEBUG=True
UPDATE_SCHEDULE_TIME=16:00
```

### 3.2 检查数据路径

确保目录结构正确:
```bash
# Windows PowerShell
dir "D:\通达信"

# 应该看到:
# sh000001_D.csv
# sz000858_D.csv
# stocks.csv
```

---

## 🚀 步骤4: 一键启动

### Windows 用户

**方案A: 双击运行批处理脚本**
```bash
# 进入StockPy根目录，双击:
run.bat
```

**方案B: PowerShell运行**
```powershell
cd f:\StockPy\backend
python -m uvicorn app.main:app --reload
```

### Linux/macOS 用户

```bash
cd f:\StockPy\backend
./run.sh
# 或
python -m uvicorn app.main:app --reload
```

---

## ✅ 步骤5: 验证运行

### 5.1 检查服务启动

```
✓ Uvicorn running on http://0.0.0.0:8000
✓ 数据库初始化完成
✓ 定时任务调度器启动完成
```

### 5.2 测试API

在浏览器访问:

```
http://localhost:8000/docs
```

点击 "Try it out" 测试:

#### 1️⃣ **测试K线查询**
```
GET /api/kline/list?code=sh000001&period=D&limit=10
```

**预期返回**:
```json
[
  {
    "code": "sh000001",
    "date": "2024-12-03",
    "close_price": 3150.00,
    "change_percent": 1.61,
    ...
  },
  ...
]
```

#### 2️⃣ **测试情绪数据**
```
GET /api/sentiment/today
```

**预期返回**:
```json
{
  "date": "2024-12-03",
  "sentiment_score": 65.5,
  "sentiment_level": "乐观",
  "up_count": 1234,
  "down_count": 1234,
  ...
}
```

---

## 📂 快速准备示例数据

如果您暂时没有通达信数据，可以用我生成的示例数据快速测试。

我会创建以下示例CSV文件供您直接使用:
1. `sh000001_D.csv` - 上证指数6个月数据
2. `sz000858_D.csv` - 五粮液6个月数据  
3. `stocks.csv` - 5只股票信息

**放置方式**:
```
D:\通达信\
├── sh000001_D.csv
├── sz000858_D.csv
├── stocks.csv
```

---

## 🔄 定时更新任务

配置完成后，系统会:

✅ **每日下午4点自动**:
1. 读取本地CSV文件的最新数据
2. 导入数据库
3. 计算技术指标
4. 生成大盘情绪数据

```
[2024-12-03 16:00:01] ================================================== 
[2024-12-03 16:00:01] 开始执行每日市场数据更新（下午4点）
[2024-12-03 16:00:01] 1. 获取股票列表...
[2024-12-03 16:00:02] 获取到 5 只股票
[2024-12-03 16:00:02] 2. 更新K线数据...
[2024-12-03 16:00:03] ♦ K线数据更新完成，新增 42 条记录
[2024-12-03 16:00:04] 3. 计算大盘情绪数据...
[2024-12-03 16:00:04] ♦ 情绪数据已保存
[2024-12-03 16:00:04]   - 上涨家数：1234
[2024-12-03 16:00:04]   - 下跌家数：1234
[2024-12-03 16:00:04]   - 情绪评分：65.5 (乐观)
```

---

## 🎨 前端表现

启动后,前端将自动显示:

1. **仪表板** - 今日情绪评分、上涨下跌统计
2. **K线图** - 输入代码查看价格走势
3. **情绪趋势** - 历史情绪指数图表
4. **回测界面** - 策略回测框架

---

## 📊 通达信数据导出完整步骤

### 详细版本（附截图说明）

1. **打开通达信** → 进入"行情"界面
   
2. **选择股票** → 比如"上证指数"
   
3. **右键菜单** → 点击"导出数据"
   
4. **导出设置**:
   - ✅ 时间范围: 最近1年 (或全部历史数据)
   - ✅ 数据周期: 日线 (D)
   - ✅ 包含字段: 开盘、最高、最低、收盘、成交量、成交额

5. **导出格式** → 选择 CSV 格式
   
6. **保存文件** → 命名为 `sh000001_D.csv`，保存到 `D:\通达信\`

7. **重复步骤** → 导出其他需要的股票

8. **验证** → 打开CSV文件，确保有数据

---

## ⚡ 常见问题

### Q: 找不到CSV文件？
**A**: 检查路径是否正确
```powershell
# 列出目录内容
dir "D:\通达信" -Filter "*.csv"
```

### Q: CSV格式不对？
**A**: 确保包含这些列:
```
date,code,open_price,high_price,low_price,close_price,volume,amount,change_percent
```

### Q: 数据没有更新？
**A**: 
1. 检查日志输出
2. 确认DATA_SOURCE_TYPE=local
3. 确认LOCAL_DATA_PATH正确

### Q: 如何每天自动更新数据？
**A**: 在通达信设置定时导出，或在Windows任务计划程序中配置

---

## 🎯 下一步

1. ✅ **今天**: 导出通达信数据 → 配置项目 → 启动运行
2. ✅ **明天**: 查看数据 → 验证情绪计算 → 调整参数
3. ✅ **本周**: 集成更多数据源 → 前端优化

---

## 📞 需要帮助？

我可以为您:
- 生成示例CSV数据文件 ✅
- 写自动导入脚本
- 配置数据更新任务
- 优化前端显示

---

**现在就开始吧！** 🚀
